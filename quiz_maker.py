class QuizMaker:
    def create_quiz(self, content, num_questions=5):
        # Placeholder: generate simple quiz
        quiz = [f"Question {i+1} about {content[:30]}" for i in range(num_questions)]
        return quiz
